Steps for EDA Notebook

You must have uv installed.


```
uv venv
source .venv/bin/activate # for linux
.venv/Scripts/activate # for windows
uv sync
```

Then create a .env file and add URL = <YOUR_FILE_URL>

Continue with EDA , run all.